<div id="primary">
  <h1 class="page-title">分类: <%= category %></h1>
  
  <% if (posts && posts.length > 0) { %>
    <% posts.forEach((post, index) => { %>
      <div class="post" data-index="<%= index %>">
        <a href="<%= config.site.root %>post/<%= post.slug %>" class="pview" style="background-image: url(<%= post.cover %>);">
          <img src="<%= post.cover %>" alt="<%= post.title %>" />
        </a>
        
        <div class="else">
          <p><%= helpers.formatDate(post.date, 'MMMM Do, YYYY') %></p>
          <h3><a href="<%= config.site.root %>post/<%= post.slug %>" class="pview"><%= post.title %></a></h3>
          <p><%= post.excerpt %></p>
          
          <div class="here">
            <% if (post.tags && post.tags.length > 0) { %>
              <span class="icon-tag"></span>
              <% post.tags.forEach((tag, i) => { %>
                <a href="<%= config.site.root %>tag/<%= tag %>"><%= tag %></a><% if (i < post.tags.length - 1) { %>, <% } %>
              <% }); %>
            <% } %>
          </div>
        </div>
      </div>
    <% }); %>
  <% } else { %>
    <p>此分类下暂无文章</p>
  <% } %>
  
  <!-- Pagination -->
  <% if (totalPages > 1) { %>
    <div id="pager">
      <% if (hasPrev) { %>
        <a href="<%= config.site.root %>category/<%= category %>?page=<%= prevPage %>" class="pview">← PREV</a>
      <% } %>
      
      <span style="margin: 0 20px; color: #999;">Page <%= currentPage %> / <%= totalPages %></span>
      
      <% if (hasNext) { %>
        <a href="<%= config.site.root %>category/<%= category %>?page=<%= nextPage %>" class="pview">NEXT →</a>
      <% } %>
    </div>
  <% } %>
</div>
